# Hoisted linker: this is a vendored (non-published) monorepo consumed by a
# single Vite build. Hoisting avoids phantom-dependency resolution issues for
# JSX source packages and keeps one node_modules tree.
node-linker=hoisted
